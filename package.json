{
  "name": "rivieraarrival",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-journal.mjs",
    "test": "node scripts/build-journal.mjs && node scripts/check-journal.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.12"
  }
}
